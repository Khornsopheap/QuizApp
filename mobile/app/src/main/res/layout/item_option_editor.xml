<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/slate_100"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <CheckBox
            android:id="@+id/correctToggle"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:button="@drawable/checkbox_correct_selector" />

        <EditText
            android:id="@+id/optionInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:hint="Option text..."
            android:padding="8dp"
            android:textColor="@color/slate_800"
            android:textSize="14sp" />

        <ImageView
            android:id="@+id/deleteOptionBtn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="8dp"
            android:src="@drawable/ic_delete"
            app:tint="@color/slate_200" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
